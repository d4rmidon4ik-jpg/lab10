# PROMPT_LOG.md

## Задание 4: FastAPI-сервис, вызывающий Go-сервис через HTTP

### Промпт 1
**Инструмент:** Claude
**Промпт:** "Напиши FastAPI-сервис который проксирует запросы к Go-сервису.
Нужны эндпоинты /go/ping, /go/calculate, /go/items/:id.
HTTP-клиент должен быть общим, не создаваться на каждый запрос."
**Результат:** Получил структуру с lifespan и отдельным client.py.
Не понравилось что get_client() не проверял None — добавил RuntimeError.

### Промпт 2
**Инструмент:** Claude
**Промпт:** "Напиши тесты с моком транспорта httpx, чтобы не поднимать реальный Go-сервис.
Покрой happy path, деление на ноль, отсутствующие поля, отрицательные id."
**Результат:** Получил MockTransport. Пришлось вручную добавить тест на нулевой id (был только -1).

### Промпт 3
**Инструмент:** Claude
**Промпт:** "Перепиши тесты на интеграционные — без моков, с реальным вызовом Go-сервиса. Используй TestClient с lifespan. Покрой все эндпоинты: /health, /go/ping, /go/calculate (add, sub, mul, div, division by zero, invalid op, missing fields, negative numbers), /go/items/:id (valid, not found, zero). Добавь unit-тест на Pydantic валидацию."
**Результат:** Получил интеграционные тесты с TestClient и фикстурой client.Пришлось вручную добавить:проверку на отрицательные числа в calculate,тест test_proxy_item_zero (изначально был только test_proxy_item_not_found с -1),уточнить что invalid_op и missing_fields возвращают 422 (Pydantic валидация), а не 400,добавить unit-тест test_calculate_request_model_rejects_invalid_op

### Итого
- Количество промптов: 3
- Что исправлял вручную: добавил test_proxy_item_zero,добавил проверку None в get_client(),добавил test_proxy_calculate_negative_numbers,добавил unit-тест на Pydantic валидацию,настроил pytest-asyncio для асинхронных тестов.
- Время: ~25 мин

---

## Задание 6: Сравнение скорости FastAPI и Gin под нагрузкой

### Промпт 1
**Инструмент:** Claude
**Промпт:** "Напиши bash-скрипт для wrk, который тестирует GET /ping и POST /calculate
на обоих сервисах с одинаковыми параметрами (потоки, соединения, время)."
**Результат:** Скрипт получился, но POST через heredoc не сработал с первого раза —
wrk принимает Lua-скрипт файлом, а не stdin таким образом.

### Промпт 2
**Инструмент:** Claude
**Промпт:** "Перепиши POST-секцию через отдельный Lua-файл или через /dev/stdin с правильным heredoc"
**Результат:** Исправил на inline heredoc с LUA-маркером. Проверил локально — работает.

### Итого
- Количество промптов: 2
- Что исправлял вручную: синтаксис heredoc для Lua в wrk
- Время: ~15 мин

---

## Задание 8: Swagger для FastAPI и OpenAPI для Gin

### Промпт 1
**Инструмент:** Claude
**Промпт:** "Добавь swaggo-аннотации к хендлерам Go и подключи gin-swagger.
FastAPI Swagger идёт из коробки — нужно только правильно заполнить title и description."
**Результат:** Аннотации добавлены. Забыл добавить импорт `_ "lab10/go-service/docs"` в server.go.

### Промпт 2
**Инструмент:** Claude
**Промпт:** "Почему swagger не работает после swag init? Выдаёт 404 на /swagger/index.html"
**Результат:** Проблема была в отсутствующем blank-импорте docs-пакета. Добавил в server.go.

### Итого
- Количество промптов: 2
- Что исправлял вручную: blank import docs в server.go, путь к swag в PATH
- Время: ~20 мин